#include<stdio.h>
void main()
{ float answer,base,height;
printf("Enter the base : ");
scanf("%f",&base);
printf("Enter the height : ");
scanf("%f",&height);
answer=0.5*base*height;
printf("Area is %f",answer);
}
